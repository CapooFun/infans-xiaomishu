# Infans 开源工程 · Agent 说明

这是给陌生使用者的短治理。原件优先、派生视图、稳定 ID、受控写回。不要把私人授权、真实账号或作者机器路径写进本仓库。

`Infans_Studio` 对应示例库根：禁止新建 `docs/`、`tmp/`、`派生数据/`。文档与来源包进入所属模块，短期过程文件使用系统临时目录，运行状态只进入 `Infans_Studio/00_本地工作台/派生数据/`。仓库根 `docs/` 只放给使用者看的说明，不要在示例库根再复制一份。

请打开本仓库根来协作。Cursor 会自动加载 `.cursor/rules/` 里三份公开适配（任务标题用中文、待办分级、说人话）；其他 Agent 直接遵守本文。不要把作者私人规则整份拷进来。

目前作者更推荐用 GPT 6 mini 做本项目的首次搭建。

## 对话、待办与说法

- 默认简体中文。先讲结论，再讲依据和下一步；写完整句子。界面和功能名优先用普通人能懂的说法。使用者指出讲得不像人话时，立刻改写，不争辩。
- 任务窗口标题用 4–16 字中文概括「项目／功能 + 整场任务的核心目标」。开场最多自动定一次；已经是合适的中文名后不要再改。只有使用者当次明确说改名，才再改。
- 待办未标明等级按 C。S＝重要且紧急，A＝重要不紧急，B＝紧急不重要，C＝不重要且不紧急。AI 不按措辞自行提级。需要使用者亲自看观感、手感或内容口径的验收默认 A。未完成任务四级互斥，只能切换，不能清空。`AI·` 自动任务能不能跑，不由 SABC 冒充授权。
