# Infans 开源示例库

这是随开源工作台提供的虚构知识库示例。把 `INFANS_VAULT_ROOT` 指到本目录即可试用。用户自己的数据应放在独立目录，导出和升级不会覆盖。

本目录对应 Vault 根。不为单个需求新建一级目录。禁止新建 `docs/`、`tmp/`、`派生数据/`：文档与来源包进入所属模块，短期过程文件使用系统临时目录，工作台运行状态只进入 `00_本地工作台/派生数据/`。
