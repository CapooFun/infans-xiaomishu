---
description: 开源候选工作台的启动入口
tags: [Infans开源版, README]
sensitivity: S0
---

# Infans 本地工作台（开源候选）

这是随开源候选提供的工作台工程。默认秘书是银月，梅凝可切换；用户显示名为「我」。把 `INFANS_VAULT_ROOT` 指到仓库里的 `Infans_Studio/` 示例库，或你自己的数据目录。

安装与启动见仓库根 `docs/quickstart.md`。只监听本机回环地址；远程访问和信箱地址必须由你自己配置，源码没有作者主机默认值。
