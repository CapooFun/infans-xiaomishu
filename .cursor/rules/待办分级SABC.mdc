---
description: 待办使用 SABC；普通任务缺省为 C，AI 不擅自提级
alwaysApply: true
---

# 待办分级 · SABC

完整边界见仓库根 `AGENTS.md`「对话、待办与说法」。

- 象限写在类型后面，用 `A：` 或 `象限：A`。禁止 `｜SABC：A`、`｜等级：A`。
- 使用者点了才写象限；没点就留空，系统按 C，不要替他写 C。
- 需要使用者亲自看观感、手感或内容口径的验收任务默认 A。
- 给人看的日期写在时间位，用 `9/10 ·`。不要只把日期写成 `｜计划日期：` 放在标题后面。
- 四级互斥，只能切换，不能清空。
- `AI·` 不能用 SABC 冒充自动授权。
