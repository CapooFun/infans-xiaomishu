---
description: 用户可见的 Agent 任务标题默认使用简短中文
alwaysApply: true
---

# 任务标题用中文

完整边界见仓库根 `AGENTS.md`「对话、待办与说法」。

- 任务窗口标题用 4–16 字中文概括「项目／功能 + 整场任务的核心目标」。开场最多自动定一次；当前工具若支持改标题（Cursor 用 `rename_chat`），一场对话最多调用一次。已经是合适的中文名后不要再改。只有使用者当次明确说改名，才再改。
- 已经改过一次，或标题已经是能概括整场任务的中文名时，不要再调用。
- 后来看清目标、对话变长、补一步、任务转到另一件事，都不构成第二次调用。只有使用者当次明确说改名，才再改。
